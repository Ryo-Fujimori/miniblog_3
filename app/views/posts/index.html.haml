%h1 ポスト一覧
%ul
  - @posts.each do |post|
    %li
      %h2= post.title
      %p= post.content
      %p= link_to 'Edit', edit_post_path(post)
      -# %p= link_to 'Delete', post, method: :delete, data:  confirm: 'Are you sure?' 
